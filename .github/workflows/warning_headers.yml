name: Deprecated Routes

on: workflow_dispatch

jobs:
  build-and-test:
    env:
      INTEGRATION_TESTS_PROJECT: '.\src\EVEOnline.ESI.Communication.IntegrationTests\EVEOnline.ESI.Communication.IntegrationTests.csproj'
      ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
      
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux
            
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
      
    - name: Test
      run: dotnet test $INTEGRATION_TESTS_PROJECT --filter "Category=DeprecatedHeaderTests" --environment AccessToken=$ACCESS_TOKEN
